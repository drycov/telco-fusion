import { Request, Response } from 'express';
import UserModel from '../models/UserModel';

const getUsersPage = (req: Request, res: Response) => { }
const addNewUser = (req: Request, res: Response) => { }
const editCurrentUser = (req: Request, res: Response) => { }
const lockCurrentUser = (req: Request, res: Response) => { }

export default { getUsersPage }
